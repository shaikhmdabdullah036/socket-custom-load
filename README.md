# Socket Server with Custom Load

## How to run

- Run `bun install` or `yarn` or `npm i` to install dependencies.
- Run `bun start` or `yarn start` or `npm start` to start the server.

## How to test

- Connect your client to `localhost:8080` or `<LOCAL_IP>:8080` using a socket client.
